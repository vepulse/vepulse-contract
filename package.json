{
  "name": "vepulse-contract",
  "version": "1.0.0",
  "description": "VeChain smart contracts for Vepulse - polls, surveys, and projects platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network vechain_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network vechain_mainnet"
  },
  "keywords": ["vechain", "smart-contracts", "polls", "surveys"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@vechain/sdk-hardhat-plugin": "^2.0.4",
    "@vechain/sdk-core": "^1.0.0",
    "@vechain/sdk-network": "^1.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
